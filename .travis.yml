
language: rust
rust: stable
cache: cargo
os: windows

install:
  - bin/install-python.bat
  - python3 --version
  - python3 -m pip install pip --upgrade
  - python3 -m pip install winreg
  - bin/install-roblox.bat
  - cargo install run-in-roblox
  - python3 bin/rbx-studio-sign-in.py $ROBLOXSECURITY

script:
  - echo "Run tests!"
  - run-in-roblox place.rbxlx -s bin/run-tests.lua

branches:
  only:
  - master